import {createContext} from "react"

const Context = createContext(null)

export default function ContextProvider({alpha}) {
    
    return (
        <Context.Provider>

        </Context.Provider>
    )
}